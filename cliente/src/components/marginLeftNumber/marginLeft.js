import React from 'react'
import './marginLeft.scss'
const MarginLeft = ({ page }) => {
    return (
        <section className="brandMargin">
            <p>A</p>
            <p className="page">{page}</p>
        </section>
    )
}

export default MarginLeft
